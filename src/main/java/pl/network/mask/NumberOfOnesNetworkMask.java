package pl.network.mask;

import pl.network.AbstractAddress;

public class NumberOfOnesNetworkMask extends AbstractAddress<Byte> {
  public NumberOfOnesNetworkMask (Byte address) {
    super(address);
  }
}
