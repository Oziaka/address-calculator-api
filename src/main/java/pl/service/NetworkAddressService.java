package pl.service;

import org.springframework.stereotype.Service;
import pl.address.NetworkAddress;
import pl.address.NetworkMask;
import pl.address.Type;

@Service
public class NetworkAddressService {
  public NetworkAddress getNetworkAddress (String address, Type addressType, NetworkMask networkMask) {
    return null;
  }
}
